import TopBar from "./header/TopBar"
import MainHead from "./header/MainHead"

const Header = () => {
    return (
        <header>
            <TopBar />
            <MainHead/>
        </header>
    )
}

export default Header
